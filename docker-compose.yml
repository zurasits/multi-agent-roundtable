version: '3.8'

services:
  app:
    build: .
    container_name: multi_agent_roundtable
    ports:
      - "8501:8501"
    volumes:
      - .:/app
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
    stdin_open: true
    tty: true
